{
  "name": "movie-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "npm run build && node ./build/app.js",
    "dev": "parcel app.js --target node",
    "clean": "rm -rf dist build",
    "build": "npm run clean && parcel build app.js -d build --target node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "puppeteer": "^1.9.0",
    "parcel-bundler": "^1.10.3"
  }
}
