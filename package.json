{
  "name": "github-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "npm run build && node ./build/app.js",
    "dev": "parcel *.js --target=node",
    "prod": "parcel *.js --target=node detailed-report",
    "clean": "rm -rf dist build",
    "build": "npm run clean && parcel build app.js -d build --target node",
    "test": "jest --watch"
  },
  "jest": {
    "modulePathIgnorePatterns": [
      ".dist/*"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.6.0"
  },
  "dependencies": {
    "D": "^1.0.0",
    "parcel-bundler": "^1.10.3",
    "puppeteer": "^1.9.0",
    "sinon": "^7.1.1",
    "sinon-test": "^2.4.0"
  }
}
